<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@drawable/bg_slot_card"
    android:foreground="?attr/selectableItemBackground">

    <!-- Top row: favicon tile + url hint -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <FrameLayout
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@drawable/bg_icon_tile">

            <ImageView
                android:id="@+id/slotIcon"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_public"
                android:contentDescription="@null" />
        </FrameLayout>

        <TextView
            android:id="@+id/slotHint"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:gravity="end"
            android:maxLines="1"
            android:ellipsize="end"
            android:textColor="#3B5BDB"
            android:textSize="12sp" />
    </LinearLayout>

    <TextView
        android:id="@+id/slotLabel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:maxLines="1"
        android:ellipsize="end"
        android:textColor="#1A1C1E"
        android:textSize="17sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/slotUrl"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:maxLines="2"
        android:ellipsize="end"
        android:textColor="#5B5E62"
        android:textSize="13sp" />
</LinearLayout>
